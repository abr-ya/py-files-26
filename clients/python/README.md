# py-files-cli

Console client for upload and download (MVP).

Install:

```bash
cd clients/python
python -m venv .venv && source .venv/bin/activate
pip install -e .
```

Commands are implemented in phase 2+ (`pyfiles --help`).
